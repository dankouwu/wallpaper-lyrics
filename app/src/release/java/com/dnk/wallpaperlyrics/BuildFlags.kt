package com.dnk.wallpaperlyrics

// Hand written instead of the generated build config so DEBUG is a compile time constant,
// allowing release builds to eliminate the diagnostics branch.
object BuildFlags {
    const val DEBUG: Boolean = false
}
